# Tiny Boilerplate
A very minimalistic web project boilerplate.

Save a few clicks with this simple project template.

If you need a lot of features, this template is probably not best for you.

## Includes
- jQuery
- Normalize.css

## Usage
Run the following command to install dependencies:
```bash
yarn install
```

> Note: you can also use `npm install` for this.
